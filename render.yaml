services:
  - type: web
    name: who-outbreak-app
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: OPENAI_API_KEY
        value: your-openai-api-key  # Replace with your actual key